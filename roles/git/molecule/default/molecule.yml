---
dependency:
  name: galaxy
  enabled: false
driver:
  name: default
  options:
    ansible_connection_options:
      ansible_connection: local
platforms:
  - name: localhost
provisioner:
  name: ansible
  inventory:
    host_vars:
      localhost:
        ansible_connection: local
  config_options:
    defaults:
      callbacks_enabled: profile_tasks,timer
    ssh_connection:
      pipelining: false
scenario:
  test_sequence:
    - dependency
    - converge
    - idempotence
    - verify
verifier:
  name: ansible
